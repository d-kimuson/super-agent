{
  "$schema": "https://unpkg.com/release-it@17/schema/release-it.json",
  "plugins": {
    "release-it-pnpm": {
      "inFile": "CHANGELOG.md"
    }
  },
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}"
  },
  "github": {
    "release": true
  },
  "npm": {
    "publish": true,
    "skipChecks": false
  },
  "hooks": {
    "before:init": ["pnpm i --frozen-lockfile --ignore-scripts", "pnpm lint", "pnpm typecheck"],
    "after:release": "echo Successfully released ${name} v${version} to ${repo.repository}."
  }
}
